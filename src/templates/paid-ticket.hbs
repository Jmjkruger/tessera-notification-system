{{> header}}

          <!-- Event image -->
          {{#if eventImageUrl}}
          <tr>
            <td align="center" style="padding:0 24px 16px;">
              <img src="{{eventImageUrl}}" alt="{{eventName}}" width="256" style="display:block; max-width:256px; height:auto; border-radius:8px; object-fit:cover;" />
            </td>
          </tr>
          {{/if}}

          <!-- Greeting -->
          <tr>
            <td style="padding:8px 24px 4px;">
              <h1 style="margin:0; font-size:20px; font-weight:700; color:#1f2937; line-height:1.3;">
                {{#if multipleTickets}}Your tickets are ready!{{else}}Your ticket is ready!{{/if}}
              </h1>
            </td>
          </tr>
          <tr>
            <td style="padding:4px 24px 20px;">
              <p style="margin:0; font-size:14px; color:#6b7280; line-height:1.5;">
                Thank you for your purchase! Here {{#if multipleTickets}}are your {{ticketCount}} tickets{{else}}is your ticket{{/if}} for <strong style="color:#1f2937;">{{eventName}}</strong>.
                {{#if orderNumber}}
                <br /><span style="font-size:12px; color:#9ca3af;">Order {{orderNumber}}</span>
                {{/if}}
              </p>
            </td>
          </tr>

          {{> ticketCards}}

          <!-- Present this email note -->
          <tr>
            <td style="padding:0 24px 24px;">
              <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background-color:rgba(240, 90, 40, 0.04); border:1px solid rgba(240, 90, 40, 0.15); border-radius:8px;">
                <tr>
                  <td style="padding:14px 16px;">
                    <p style="margin:0; font-size:13px; color:#1f2937; line-height:1.5;">
                      <strong>Present this email at the event.</strong> Show the QR code{{#if multipleTickets}}s{{/if}} above at the entrance for scanning. Keep this email safe &mdash; {{#if multipleTickets}}these are your tickets{{else}}it is your ticket{{/if}}.
                    </p>
                  </td>
                </tr>
              </table>
            </td>
          </tr>

{{> footer}}
