# TNS Server
PORT=4001

# Shared secret — must match TESSERA_TNS_API_KEY in wp-config.php
TNS_API_KEY=change-me-to-a-long-random-string

# WordPress REST API (staging URL during testing, live URL for production)
WP_API_URL=https://staging.tessera.co.zm/wp-json

# AWS SES
AWS_REGION=eu-central-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SES_SENDER_EMAIL=notifications@tessera.co.zm
SES_SENDER_NAME=Tessera Tickets
